#include <stdio.h>
/**
 * main - 4-puts.c
 * description - write a C program that prins exactly
 *
 * Return: always (0) (success);
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
		return;
}
